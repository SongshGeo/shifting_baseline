# Constants · `shifting_baseline.constants`

!!! info "EN / 中文"
    Year boundaries (`STAGE1`, `END`), the level→value `MAP`, and other project-wide constants.
    <br>**中文：** 年份边界（`STAGE1`、`END`）、等级→数值映射 `MAP` 等全项目常量。

The reference below is generated from the source docstrings, so it always matches
the installed code. 以下内容由源码 docstring 自动生成，与代码保持同步。

::: shifting_baseline.constants
