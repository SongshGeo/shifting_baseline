# Filters & classification · `shifting_baseline.filters`

!!! info "EN / 中文"
    Sliding-window re-standardisation (`calc_std_deviation`) and continuous→ordinal classification (`classify`).
    <br>**中文：** 滑动窗口再标准化（`calc_std_deviation`）与连续值→序数分级（`classify`）。

The reference below is generated from the source docstrings, so it always matches
the installed code. 以下内容由源码 docstring 自动生成，与代码保持同步。

::: shifting_baseline.filters
